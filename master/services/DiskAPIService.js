var assert 	= require('assert');

exports.download = function (options, cb) {
/*	console.log("options options options options options options options");
	console.log(options);

	// cb(options);

	
	var file = sails.config.appPath + '/../api/files/'+options.name;
*/
	//var filename = path.basename(file);
	//console.log(filename);	

	//var mimetype = mime.lookup(file);
	//console.log(mimetype);	
	
	//res.setHeader('Content-disposition', 'attachment; filename=69312_154491711256235_4945790_n.jpg');
	//res.setHeader('Content-type', 'image/jpg');
	
	// var filestream = fs.createReadStream(file);
 //  	cb(filestream.pipe(res));


};